export const speakText = (text: string) => { if (!text) return; if (typeof window === "undefined") return; if ((window as any).AndroidBridge?.speak) { (window as any).AndroidBridge.speak(text); return; } if ((window as any).webkit?.messageHandlers?.speak) { (window as any).webkit.messageHandlers.speak.postMessage(text); return; } if (window.speechSynthesis) { const u = new SpeechSynthesisUtterance(text); window.speechSynthesis.speak(u); } };
